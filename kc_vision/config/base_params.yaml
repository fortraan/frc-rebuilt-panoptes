tag_consensus:
  ros__parameters:
    pose_prefixes:
      - "front_camera/"
      - "left_camera/"
      - "right_camera/"
      - "back_camera/"
    max_estimate_age_ms: 500
    update_interval_ms: 20
    consensus_frame_id: "visual_pose_estimate"
